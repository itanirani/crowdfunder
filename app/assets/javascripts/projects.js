$(document).on('ready page:load', function() {
	
	// $('.donate').on('click', function(event) {
	// 	var self = $(this),
	// 		// reward = self.closest('.reward'),
	// 		// rewardId = reward.data('reward-id'),
	// 		url = self.attr('href');

	// 	event.preventDefault();

	// 	alert("Thank you for Donating to the cause, we are closer to our goal!");
		
	// 	$.get(url);
	// 	// rewardAmount = reward.find('.reward-amount').text();

	// 	// alert(rewards.eq(rewards.index(this) + 1)[0].outerHTML);  //next element, be it next sibling or not

	// 	// $("#need_money").html(
	// 	// 	parseInt($('#need_money').html()) - parseInt($('.reward-amount').html())
	// 	// );

	// });

	
});